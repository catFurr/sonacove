# Keycloak & Postgres Credentials
KC_DB_PASSWORD=your_strong_postgres_password_here
KC_ADMIN_USER=admin
KC_ADMIN_PASSWORD=your_strong_keycloak_admin_password_here

# Keycloak Hostname (ensure this is resolvable where Keycloak is accessed from the outside world)
# This is used by Keycloak itself and for Traefik routing.
KC_HOSTNAME=auth.sonacove.com

# URL for the registration API validator (if applicable)
KC_REGISTRATION_API_URL=https://sonacove.com/api/registration-flow

# Keycloak Webhook Setup Configuration
# This is the realm the webhook will be configured for by the setup script.
KC_REALM_NAME=jitsi # Or your specific realm name, e.g., master, your-app-realm

# The URL your application will listen on for webhook events from Keycloak.
KC_WEBHOOK_URL=https://sonacove.com/api/keycloak-webhook

# A strong, unique secret that Keycloak will use to sign webhook requests.
# Your application should verify this secret to ensure requests are genuinely from Keycloak.
KC_WEBHOOK_SECRET=generate_a_very_strong_random_secret_here_please

# Comma-separated list of Keycloak event types to subscribe the webhook to.
# Examples:
#   - Just registration: REGISTER
#   - Registration and login: REGISTER,LOGIN
#   - All user events: USER_EVENT (this might be a category, check Keycloak docs for exact event type names)
#   - For specific events from the keycloak-events provider: consult its documentation for exact type names if they differ from standard Keycloak event types.
# The script currently defaults to "UPDATE_PROFILE,VERIFY_EMAIL" if this is empty or not set.
KC_WEBHOOK_EVENT_TYPES=UPDATE_PROFILE,VERIFY_EMAIL

# --- Optional ---
# If you are using Traefik's ACME feature for SSL certificates with a DNS challenge or need a specific email:
ACME_EMAIL=your-email@example.com
